# HANDOFF — autonomous run 2026-06-18 → 2026-06-20 01:00 UTC

User away ~38h. Goal: keep all 4 GPUs busy AND make real progress toward the CompPlan (jumpy
world model) reproduction. Boxes: g3 = inst 41135265 (2×4090), g3090 = inst 41213845 (2×3090).

## What is running (autonomous, no human needed)
1. **GPU keep-busy loop** — `scripts/ghm_loop.sh` (detached on EC2, pid in
   `exp/tdmpc_glass/ghm/loop.pid`). Scans all 4 GPUs; whenever one is free it launches a
   horizon-conditioned **GHM** run via `/root/ghm/launch_infom.sh ... --agent=agents/ghm.py`.
   - **Env rotation read from a FILE**: `exp/tdmpc_glass/ghm/envs.txt` (one OGBench env/line).
     Seeded with the 5 cube-single tasks. The work agent appends **antmaze** here once unblocked →
     the loop folds antmaze into the rotation with NO restart.
   - **Disk-safety**: harvests eval CSVs to `exp/tdmpc_glass/ghm/mirror/`, then prunes
     `params_*.pkl` checkpoints >120 min old (or all if <5G free). 81 MB/checkpoint × ~100 runs
     would otherwise fill the 50G disks.
   - **Watchdog cron** (`crontab -l`, every 10 min) restarts the loop if its pid dies.
2. **Work agent** (background, started 2026-06-18): two jobs —
   - **P1 Unblock antmaze** (the long-horizon test that actually probes the jumpy hypothesis):
     InFOM needs a `-ft-` dataset antmaze lacks; agent finds the minimal path (pretrain-only eval /
     reuse pretrain dataset as ft / minimal main.py patch), VERIFIES an antmaze GHM run produces
     `episode.success`, then **appends antmaze to envs.txt** + syncs any code to both boxes.
   - **P2 Plan-over-policies planner** (CompPlan gap c): implements `planning/compplan_planner.py`
     + smoke test. This is the missing third piece (GHM model exists; planner doesn't).

## What to expect on return (2026-06-20 01:00 UTC)
- **GPUs busy the whole time** (cube GHM guaranteed; antmaze GHM too if P1 succeeded).
- **A GHM-vs-InFOM cube-single table** with many seeds: `python3 scripts/ghm_aggregate.py` (or read
  `exp/tdmpc_glass/ghm/mirror/*/ghm_*/sd*/finetuning_eval.csv`, col `evaluation/episode.success`,
  take PEAK). Expectation: GHM ≈ InFOM on cube (short-horizon → jumpy not expected to help here).
- **Antmaze GHM results IF unblocked** — the meaningful long-horizon signal. Check
  `exp/tdmpc_glass/ghm/mirror/*/ghm_*antmaze*/`. This is the headline to look for.
- **A planner implementation** (`ghm_repro/infom/planning/`) ready to wire into a full
  plan-over-policies eval — likely NOT a finished CompPlan-Table-1 reproduction yet (multi-day).
- Possible agent-failure modes (honest): antmaze unblock may not fully work (then loop just kept
  grinding cube — GPUs still busy, but no antmaze numbers); planner may be smoke-only.

## Metric reminder
`episode.success` ∈ [0,1] = fraction of ~50 eval episodes reaching the goal (headline). `return` ≈
−(steps), −200/len-200 = timeout failure. Report PEAK not final (finetune eval collapses late).

## To stop / change
- Stop the loop: `crontab -e` remove the ghm_loop line, then `kill $(cat exp/tdmpc_glass/ghm/loop.pid)`.
- Change what trains: edit `exp/tdmpc_glass/ghm/envs.txt` (loop picks it up next tick).
