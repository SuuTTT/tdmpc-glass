# Wrap-up for user return (~2026-06-08 02:30Z)

## Bottom line: COMPLETE NULL confirmed — no decision pending

The FingerSpin FSQ "win" I flagged overnight **resolved to Mirage #7** at higher n. No live
lead, nothing to launch — the campaign is a clean, multiply-confirmed null. The discipline of
NOT auto-scaling at n=9 was vindicated by the data itself within two hours.

**iter-16 FSQ codebook — FINAL (NULL):**
- CheetahRun (locomotion): **−230, CI [−338,−106]** → FSQ HURTS (the one robust effect).
- WalkerRun: +4, overlap → parity.
- FingerSpin: **+91 [+14,+169] @n=8 → +53 [−33,+138] @n=16, overlaps 0.** Separated at
  *exactly one snapshot* (n=8), even passed a strengthening-with-n check (n=5→8), then
  collapsed — the cruelest mirage yet, now the 7th in the write-up. (Saturation-rate hint
  81% vs 55% is underpowered, Fisher p≈0.2 — almost certainly mirage too.)

**Only defensible next step if you want to keep chasing codebooks:** the high-dim tasks where
DC-MPC actually located its wins (Dog/Humanoid) — but prior is now low and it's real compute
(~135 sps). Recommendation: fold FSQ in as the 3rd nulled mechanism + 7th mirage and ship the
null. There is no n-to-resolve cliffhanger anymore.

## The post-null campaign (4 directions you asked me to try)

| dir | idea | verdict |
|---|---|---|
| iter-14 | auxiliary abstraction losses | NULL (the Six Mirages study) |
| iter-15 | **plan in the prototype space** | **DEAD** — proto-dynamics action-blind (sensitivity 0.012 vs 1.567), MPPI objective flat (std 0.005); ratio 0.002–0.004 vs 0.5 gate. 8.4× faster planning but worthless. Crisp mechanism: the projection grouping reward-equivalent states discards action-conditional info. |
| iter-16 | **codebook replaces SimNorm** | **TASK-DEPENDENT** (above) — the live lead |
| iter-17 | exploration *through* the abstraction | **DEAD** — novel target CartpoleSparse (flat 0/3): bonus reached 745/790 (vs flat ≤3 — exploration moved!) but cleared no 800 solve; max 2/6 < 4/6 gate. Secondary finding kept. |

## Other status
- **Six Mirages draft + blog**: published, with a same-day postscript (n=31 numbers moved as
  predicted). Draft fixed-cutoff policy: §3 tables regenerate ONCE when all 12 in-flight v2
  runs mature (not yet — still 12 in flight). FingerSpin near-miss is queued to become a live
  7th-mirage-or-real-signal sidebar depending on the n≥12 verdict.
- **HopperHop**: 5M fair pair closed — neither arm entered basin (323 vs 286); in draft.
- **iter-15 + iter-17**: closed, recorded in their plan docs + memory.
- **Fleet incidents**: 1660S retired then re-enabled per you (coincided w/ now-done se-bench);
  it killed ~5 long runs across the night (requeued). 3 boxes hit disk-93% guard once
  (cleaned to 88%). Duplicate streamer killed (→1). Daemon single-master throughout.

## ICLR / venue (unchanged)
TMLR + ICLR Blogposts recommended for the null; IF the FSQ task-dependent result holds, it
could anchor a *positive* ICLR submission ("when do discrete codes help world models") —
stronger than the null alone.
